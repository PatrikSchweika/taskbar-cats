<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.taskbar-cats"
          path="/org/gnome/shell/extensions/taskbar-cats/">

    <key name="cat-count" type="i">
      <default>3</default>
      <range min="1" max="8"/>
      <summary>Number of cats</summary>
      <description>How many cats live on the dock.</description>
    </key>

    <key name="max-speed" type="i">
      <default>160</default>
      <range min="40" max="600"/>
      <summary>Top speed in pixels per second</summary>
      <description>How fast a cat can run along the dock.</description>
    </key>

    <key name="mouse-attraction" type="i">
      <default>60</default>
      <range min="0" max="100"/>
      <summary>Mouse attraction</summary>
      <description>How strongly cats are drawn to the pointer. 0 means they ignore it.</description>
    </key>

    <key name="attract-radius" type="i">
      <default>260</default>
      <range min="40" max="1200"/>
      <summary>Attraction radius</summary>
      <description>How far above the bottom of the screen the pointer can be and still interest the cats.</description>
    </key>

    <key name="scratch-icons" type="b">
      <default>true</default>
      <summary>Scratch app icons</summary>
      <description>Let cats stop at an app icon and claw at it.</description>
    </key>

    <key name="wiggle-icons" type="b">
      <default>true</default>
      <summary>Make scratched icons shake</summary>
      <description>Rock the real dock icon while a cat scratches it. Turn this off to leave the dock untouched.</description>
    </key>

    <key name="sleep-after" type="i">
      <default>20</default>
      <range min="0" max="600"/>
      <summary>Seconds of stillness before cats nap</summary>
      <description>0 means the cats never sleep.</description>
    </key>

    <key name="animation-fps" type="i">
      <default>12</default>
      <range min="4" max="30"/>
      <summary>Sprite animation frame rate</summary>
    </key>

    <key name="bed-count" type="i">
      <default>0</default>
      <range min="0" max="8"/>
      <summary>Number of cat beds</summary>
      <description>Beds placed along the floor beside the dock. A sleepy cat walks to a free one and curls up in it.</description>
    </key>

    <key name="scratcher-count" type="i">
      <default>0</default>
      <range min="0" max="8"/>
      <summary>Number of scratching posts</summary>
      <description>Posts placed along the floor beside the dock for the cats to claw at.</description>
    </key>

    <key name="bed-positions" type="ai">
      <default>[]</default>
      <summary>Where each bed stands</summary>
      <description>One entry per bed: a percentage of the floor's width from the left edge of the dock's monitor. -1, or a missing entry, places that bed automatically.</description>
    </key>

    <key name="scratcher-positions" type="ai">
      <default>[]</default>
      <summary>Where each scratching post stands</summary>
      <description>One entry per post: a percentage of the floor's width from the left edge of the dock's monitor. -1, or a missing entry, places that post automatically.</description>
    </key>

    <key name="mouse-interval" type="i">
      <default>120</default>
      <range min="0" max="3600"/>
      <summary>Seconds between mouse visits</summary>
      <description>Roughly how often a mouse runs across the floor for the cats to hunt. 0 means no mice.</description>
    </key>

    <key name="cat-palettes" type="as">
      <default>[]</default>
      <summary>Fur palette of each cat</summary>
      <description>One entry per cat: a palette name, or an empty string to take a turn through every palette. Missing entries mean the same.</description>
    </key>

    <key name="cat-names" type="as">
      <default>[]</default>
      <summary>Name of each cat</summary>
      <description>One entry per cat, shown in the settings only. Empty or missing means "Cat N".</description>
    </key>

    <key name="cat-sizes" type="ai">
      <default>[]</default>
      <summary>Size of each cat in pixels</summary>
      <description>One entry per cat. 0, or a missing entry, matches the dock's own icon size. Otherwise 16 to 128.</description>
    </key>

    <key name="toggle-hotkey" type="as">
      <default>['&lt;Control&gt;&lt;Alt&gt;c']</default>
      <summary>Hide or show the cats</summary>
      <description>Keyboard shortcut that hides every cat and brings them back. An empty list turns the shortcut off.</description>
    </key>

  </schema>
</schemalist>
